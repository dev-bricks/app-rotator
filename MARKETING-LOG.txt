# MARKETING-LOG — App Rotator (`dev-bricks/app-rotator`)

Dieses Register dokumentiert Discoverability-, Marketing-, Visual-Design- und Ökosystem-Aktivitäten (Pfad B) sowie Pfad-A-Wartungszyklen für das Repository `dev-bricks/app-rotator`.

---

## [2026-09-21] — Pfad B: Discoverability, Visual Architecture, 17-Point Bilingual Parity, Comparative Matrix & Level 1 SBOM

- **Akteur:** Antigravity / Gemini (`WORKSTATION-LG`)
- **Version:** `0.2.3` (Version gemäß T-20260920-167562623 strikt eingefroren)
- **Typ:** `GITHUBBOT_ONE_REPO_MARKETING_AND_DESIGN` (Pfad B)
- **Repo:** `dev-bricks/app-rotator` (`C:\_Local_DEV\repos\app-rotator`, Branch `master`, Remote `https://github.com/dev-bricks/app-rotator.git`)
- **Auswahlgrund:** Laut `_tools/repo_radar.py candidates --path b` ältester berechtigter Pfad-B-Kandidat der Flotte (Ruhezustand 35.5h > 24h Cooldown, letzte Pfad-B-Prüfung am 2026-09-12 vor 9 Tagen), Arbeitsbaum 100% sauber auf Branch `master`, 0 aktive Locks, strikte Lock-Freigabe bestätigt via `lock_status.py`.

### Durchgeführte Maßnahmen:

1. **GitHub-Discoverability & Topics-Sättigung:**
   - Erweiterung der GitHub-Repository-Topics auf das Plattform-Maximum von 20/20 Topics:
     `productivity`, `pyside6`, `resource-manager`, `time-slicing`, `tray-app`, `windows`, `antigravity`, `claude-desktop`, `codex`, `desktop-app`, `developer-tools`, `gpu-management`, `local-first`, `system-tray`, `vram-management`, `zero-egress`, `open-bricks`, `dev-bricks`, `fail-closed`, `python`.

2. **17-Punkte-Schnellnavigation mit reziproken dualen HTML-Ankern:**
   - Vollständige bilaterale Harmonisierung von `README.md` und `README_de.md` über 17 symmetrische Abschnitte.
   - Jeder Abschnitt verfügt über duale HTML-Anker-Aliase (`<a id="...-en"></a><a id="...-de"></a>`), sodass Querverweise und Sprachumschaltungen nahtlos an denselben Zielpositionen ankommen.

3. **Zielgruppen-Profile & High-Intent SEO-Queries:**
   - `[PERSONA-01]` Autonome KI-Desktop-Entwickler & Swarm-Operatoren (Codex, Claude, Antigravity Time-Slicing, VRAM-Bereinigung).
   - `[PERSONA-02]` Local-First- & Air-Gapped-Entwickler (100% Zero-Egress, keine Sockets, keine Telemetrie).
   - `[PERSONA-03]` Windows-Power-User & Performance-Tuner (Electron-Speicherlecks, Schonung von Ressourcen und Akku).
   - `[PERSONA-04]` Sicherheits- & Infrastruktur-Auditoren (Unprivilegierter `RunAsInvoker`-Betrieb, Fail-Closed-Filter).

4. **10-Dimensionale 5-Wege-Vergleichsmatrix vs. Alternativen:**
   - Umfassender Benchmark gegen Manuellen Task-Manager / Alt+F4, Windows Energiesparmodus, Generische Prozess-Killer / AutoHotkey-Skripte und VM- / Container-Sandboxen.
   - Direkte Verknüpfung jeder Dimension mit den Governance- und Laufzeit-Invarianten `INV-LOCAL-01` bis `INV-SLA-10`.

5. **Rechtliche Transparenz & Gesetzesdisclaimer (§ 521 BGB):**
   - Bereitstellung der formalen Open-Source-Attributionsdatei `NOTICE` (Lukas Geiger, dev-bricks, open-bricks).
   - Ergänzung des gesetzlichen Haftungsausschlusses nach deutschem Schenkungs- und Gefälligkeitsrecht (§ 521 BGB) in Abschnitt 16 beider Dokumentationen.

6. **Level 1 SBOM & Invarianten-Matrix (`THIRD_PARTY_LICENSES.md`):**
   - Re-Audit auf Stand 2026-09-21.
   - Tabellarische Invariant Cross-Reference Matrix zur lückenlosen Dokumentation der Abhängigkeits- und Sicherheitsgrenzen.
   - Zero-Copyleft-Zertifizierung und dynamische Link-Isolation für `pystray`.

7. **PEP 621 Metadaten-Härtung (`pyproject.toml`):**
   - `license-files = ["LICENSE", "NOTICE", "THIRD_PARTY_LICENSES.md"]`.
   - `Notice`-URL ergänzt.
   - Erweiterung der Discoverability-Keywords (`zero-egress`, `open-bricks`, `dev-bricks`, `fail-closed` u. a.).
   - Version `0.2.3` strikt unverändert belassen.

8. **Automatisierte Vertragstests (`tests/test_metadata.py`):**
   - Umfassender Ausbau der Testsuite zur Absicherung aller Pfad-B-Standards: 17-Punkte-Navigation, duale Anker, Personas, Vergleichsmatrix, Level 1 SBOM, NOTICE-Attribution und § 521 BGB Disclaimer.

---

## [2026-09-19] — Pfad A: Technische Hygiene, CI-Lifecycle-Workflows, Multi-Host & Lock-Härtung, PEP 621 & Vertragstest-Ausbau

- **Akteur:** Antigravity / Gemini (`WORKSTATION-LG`)
- **Version:** `0.2.3`
- **Typ:** `GITHUBBOT_ONE_REPO_CLEANER` (Pfad A)
- **Repo:** `dev-bricks/app-rotator` (`C:\_Local_DEV\repos\app-rotator`, Branch `master`, Remote `https://github.com/dev-bricks/app-rotator.git`)
- **Auswahlgrund:** Laut `_tools/repo_radar.py candidates --path a` ältester berechtigter Top-Kandidat der Flotte (Ruhezustand 172.3h > 24h Cooldown, letzte Pfad-A-Prüfung am 2026-09-11 vor 8 Tagen), Arbeitsbaum 100% sauber auf Branch `master`, 0 aktive Locks, strikte Lock-Freigabe bestätigt via `lock_status.py`.

### Durchgeführte Maßnahmen:

1. **Automatisierte Community- & CI-Lifecycle-Workflows:**
   - `.github/workflows/stale.yml`: Bereitstellung des Stale-Workflows (`actions/stale@v9`, täglicher Cron `30 1 * * *`, `timeout-minutes: 10`, Least-Privilege-Berechtigungen `issues: write`, `pull-requests: write`, automatische Kennzeichnung nach 30 Tagen Inaktivität, Schließung nach 7 Tagen).
   - `.github/workflows/welcome.yml`: Bereitstellung des Begrüßungs-Workflows für Erstbeitragende (`actions/first-interaction@v3`, `timeout-minutes: 5`, Concurrency `cancel-in-progress: true`, Least-Privilege-Berechtigungen).

2. **Multi-Host Cloud-Sync- & Lock-Härtung (`.gitignore`):**
   - Umfassender Schutz gegen Multi-Host-Synchronisationskonflikte (`*conflicted copy*`, `* (Kopie)*`, `* (Copy)*`, `*-WORKSTATION*`, `*-WORKSTATION-LG*`, `*-LAPTOP*`, `*-ASUS*`, `*-ASUS-GEI*`, `*-Mac Studio*`, `*-MacBook*`).
   - Schutz für kanonische Lock-Dateien (`LOCK`, `LOCK.*`, `LOCK*.txt`, `LOCK.user.*`, `LOCK.until.*`, `LOCK.condition.*`, `LOCK.permissions.json`, `.automation-lock`) bei verlässlichem Erhalt von Paket-Lockfiles (`!package-lock.json`).
   - Caching-Ausschlüsse (`.hypothesis/`, `.turbo/`, `.tox/`).

3. **PEP 621 Standardisierung & Testrunner-Härtung (`pyproject.toml`):**
   - Standardisierte `license-files = ["LICENSE", "THIRD_PARTY_LICENSES.md"]` unter `[project]`.
   - `[tool.pytest.ini_options]` um `minversion = "7.0"` und explizites `norecursedirs = [".git", ".pytest_cache", "__pycache__", "build", "dist", ".venv"]` gehärtet.
   - Version auf `0.2.3` angehoben.

4. **Drittanbieter-SBOM & Lizenzaudit (`THIRD_PARTY_LICENSES.md`):**
   - Vollständiger Re-Audit auf Stand 2026-09-19 v0.2.3.
   - Bestätigung des unprivilegierten Benutzerkontexts (`RunAsInvoker`) und Zero-Copyleft-Garantie (dynamische Einbindung von Pillow, psutil, pystray).

5. **Synchrone Dokumentations-, Badge- & LLM-Parität:**
   - Versionserhöhung auf `0.2.3` synchron über `pyproject.toml`, `src/app_rotator/__init__.py`, `assets/banner.svg`, `README.md`, `README_de.md`, `llms.txt`, `CHANGELOG.md` und `MARKETING-LOG.txt`.
   - Badges in beiden READMEs auf Version `0.2.3` und Prüfdatum `2026--09--19` synchronisiert.
   - `llms.txt` Last-checked Zeitstempel auf `2026-09-19` und Version `0.2.3` aktualisiert.

6. **Vertragstest-Erweiterung (`tests/test_metadata.py`):**
   - 5 neue Contract-Tests ergänzt:
     - `test_lifecycle_workflows_present()` (Prüft Existenz und Konfiguration von `stale.yml` und `welcome.yml`).
     - `test_gitignore_multihost_and_lock_defense()` (Prüft Multi-Host-Tokens und kanonische Lock-Muster).
     - `test_pyproject_pep621_license_files_and_pytest_hardening()` (Prüft `license-files`, `minversion`, `norecursedirs`).
     - `test_third_party_licenses_audit_recency()` (Prüft Re-Audit-Datum 2026-09-19 und Invarianten).
     - `test_changelog_release_023_entry()` (Prüft Release-Eintrag `0.2.3` im Changelog).
   - Alle bestehenden Tests auf Version `0.2.3` und Prüfdatum `2026-09-19` synchronisiert.

---

## [2026-09-12] — Pfad B: Header-Banner-Design, Visual Asset Integration, Audit-Refresh & Vertragstest-Parität

- **Akteur:** Antigravity / Gemini (`WORKSTATION-LG`)
- **Version:** `0.2.2`
- **Typ:** `GITHUBBOT_ONE_REPO_MARKETING_AND_DESIGN` (Pfad B)
- **Repo:** `dev-bricks/app-rotator` (`C:\_Local_DEV\repos\app-rotator`, Branch `master`, Remote `https://github.com/dev-bricks/app-rotator.git`)
- **Auswahlgrund:** Laut `_tools/repo_radar.py candidates --path b` ältester berechtigter Pfad-B-Kandidat der Flotte (27.3h Ruhezustand > 24h Cooldown, vorangegangene Pfad-A-Prüfung am 2026-09-11), Arbeitsbaum 100% sauber auf Branch `master`, 0 aktive Locks, strikte Lock-Freigabe bestätigt via `lock_status.py`.

### Durchgeführte Maßnahmen:

1. **Visuelles Header-Banner (`assets/banner.svg`):**
   - Neues responsives High-End-Vektor-Banner im Deep-Tech-Design (Dark-Theme `#080c14` bis `#141d33`) mit dezenter Dot-Grid-Matrix und Ambient-Glows.
   - Grafische Visualisierung der zyklischen Time-Slicing-Zustandsmaschine:
     - Orbits für verwaltete Provider (`Codex Desktop`, `Claude Desktop`, `Antigravity IDE`).
     - Zentraler Isolations-Core mit Rotator-Pfeil und 15s Inter-App Cooldown Gap Indicator.
     - Brand-Pills für `dev-bricks | Tooling`, `Local-First`, `Zero-Egress`, `Fail-Closed`, `Windows Tray` und `v0.2.2`.
   - Nahtlose responsive Einbindung am Beginn von `README.md` und `README_de.md`.

2. **Dokumentations-Synchronisation & Badges:**
   - Audit-Badge in `README.md` auf `Last Checked-2026--09--12` aktualisiert.
   - Audit-Badge in `README_de.md` auf `Zuletzt geprüft-2026--09--12` aktualisiert.
   - `llms.txt` Zeitstempel auf `2026-09-12` angehoben und Testanzahl synchronisiert.

3. **Vertragstest-Erweiterung (`tests/test_metadata.py`):**
   - Neuer Test `test_header_banner_asset_exists_and_linked()`:
     - Prüft physische Existenz von `assets/banner.svg` (>5 KB, valides SVG mit Title, Gradienten, Branding-Elementen).
     - Verifiziert synchrone Verlinkung in beiden Landingpages (`README.md` und `README_de.md`).
   - Zeitstempel-Zusicherungen in bestehenden Tests für `2026-09-12` aktualisiert.

---

## [2026-09-09] — Pfad B: Discoverability, Zweisprachige Landingpage, Dual-Mermaid-Visualisierung, Governance-Invarianten & Ökosystem-Parität

- **Akteur:** Antigravity / Gemini (`WORKSTATION-LG`)
- **Version:** `0.2.2`
- **Typ:** `GITHUBBOT_ONE_REPO_MARKETING_AND_DESIGN` (Pfad B)
- **Repo:** `dev-bricks/app-rotator` (`C:\_Local_DEV\repos\app-rotator`, Branch `master`, Remote `https://github.com/dev-bricks/app-rotator.git`)
- **Auswahlgrund:** Laut `_tools/repo_radar.py candidates --path b` voll berechtigt (>25.4h Ruhezustand, letzte Pfad A Prüfung am Vortag, zuvor noch nie in Pfad B geprüft), Arbeitsbaum 100% sauber auf Branch `master`, 0 aktive Locks, Cooldown-Check bestanden.

### Durchgeführte Maßnahmen:

1. **Zweisprachige README-Architektur (`README.md` & `README_de.md`):**
   - Vollständige Parität zwischen englischer und deutscher Dokumentation.
   - Bidirektionaler Sprachumschalter im Header: `[🇬🇧 English Version](README.md) | [🇩🇪 Deutsche Version](README_de.md)`.
   - Standardisierte 14-Punkte-Schnellnavigation (`Quick Navigation` / `Schnellnavigation`) mit synchronen Ankern.
   - Shields.io Badges erweitert: Status Beta, Version `0.2.2`, CI Passing, Tests `41+ passed | 100% green`, Python `3.11 | 3.12 | 3.13`, Platform `Windows`, Lizenz `MIT`, Architektur `Local-First | Zero-Egress`, Security `RunAsInvoker | Non-Elevation`, Security SLA `48h response | 5d triage`, Code Style `Ruff`, Ecosystem `dev-bricks`, Umbrella `open-bricks`, LLM-Ready `llms.txt`, Audit Last-checked `2026-09-09`.

2. **Interaktive zweisprachige Dual-Mermaid-Diagramme:**
   - `flowchart TD`: System Architecture & Subsystem State Machine (Tray UI & Context Menu -> CLI & Atomic Mailbox -> Engine Orchestrator -> Safe Process Restriction & AUMID Launcher -> External Controller Contract & Process Watchdog).
   - `sequenceDiagram`: End-to-End Rotations- & Lebenszyklus mit Autonumbering und 14 Schritten (User/CLI Start, Mailbox Ingest, Engine Mutex Lock, Codex Safe-Start pause-all, AppX Launch, stagger-resume, Active Phase Countdown, Graceful Process Terminate, Controller cancel, Cooldown Inter-App Gap, Provider Switch, Loop Reset).

3. **Tabelle der Governance- & Laufzeit-Invarianten (10 Invarianten):**
   - 10 Kern-Garantien zweisprachig verankert mit Codes:
     1. `INV-LOCAL-01`: 100% Local-First & Zero Network Egress
     2. `INV-UNPRIV-02`: Unprivileged User Space (RunAsInvoker / Non-Elevation)
     3. `INV-DRYRUN-03`: Fail-Closed & Dry-Run by Default
     4. `INV-SCOPING-04`: Path-Restricted Process Scoping
     5. `INV-ATOMIC-05`: Atomic File-State & Mailbox IPC
     6. `INV-DELEGATION-06`: External Provider Controller Contract
     7. `INV-LOCKFILE-07`: Single-Instance File Lock Protection
     8. `INV-AUMID-08`: Non-Invasive AppX Shell Activation
     9. `INV-SYNC-09`: Cloud-Sync & Conflict Resilience
     10. `INV-SLA-10`: 48h Security SLA & 5-Day Triage Guarantee

4. **Geschwister-Ökosystem & Partner-Matrix:**
   - 16 Partner-Repositories aus `dev-bricks`, `file-bricks`, `doc-bricks`, `ellmos-ai`, `entertain-and-more` und `open-bricks` tabellarisch integriert mit Domäne und Beziehungsdefinition.

5. **Zweisprachige Sicherheitsrichtlinie (`SECURITY.md`):**
   - Supported Versions Matrix auf `0.2.x` festgelegt.
   - Verbindliche 48h-Reaktions-SLA und 5-Werktage-Triage-Zusage.
   - Dachorganisations-Kontakt `security@open-bricks.org` und `lukas@open-bricks.org` neben `security@ellmos.ai` und `support@lukasgeiger.com`.

6. **CI Multi-OS & Bytecode-Gate (`.github/workflows/tests.yml`):**
   - Concurrency (`cancel-in-progress: true`), Pip-Caching, Ruff-Linting, Bytecode-Kompilierung (`python -m compileall -q src tests`) und ausführliche Testausführung (`pytest -v`).

7. **Gitignore-Härtung (`.gitignore`):**
   - Schutz vor Synchronisationskonflikten (`*-conflict-*`, `*.sync-temp-*`, `*.sync-conflict-*`, `*.conflict`, `*-CONFLIT-*`) und Lock-Dateien (`LOCK`, `LOCK.*`, `*.lock`, `LOCK*.txt`) sowie temporären Editor- und Cache-Artefakten.

8. **PEP 621 Standard-Metadaten (`pyproject.toml`):**
   - Versionserhöhung auf `0.2.2`.
   - `[tool.pytest.ini_options]` auf `addopts = "-ra -v"` standardisiert.

9. **Dritte-Partei-Lizenzinventar (`THIRD_PARTY_LICENSES.md`):**
   - Vollständiges Lizenzinventar für Pillow, psutil, pystray, pytest, ruff und setuptools angelegt.

10. **Maschinenlesbarer Kontext (`llms.txt`):**
    - Header aktualisiert auf Stand `2026-09-09`, Version `0.2.2`, 10 Governance-Invarianten, 16 Partner-Repositories, Referenz auf `MARKETING-LOG.txt`.

11. **CHANGELOG.md & automatisierte Vertragstestsuite (`tests/test_metadata.py`):**
    - `CHANGELOG.md` um Pfad-B-Release `[0.2.2] — 2026-09-09` erweitert.
    - `tests/test_metadata.py` von 8 auf 15 Contract-Tests erweitert (100% grün, 48/48 Gesamt-Tests bestanden).

---

## [2026-09-11] — Pfad A: Technische Hygiene, CI-Matrix-Härtung, PEP 621 Standard-Metadaten, Ruff-Modernisierung & Vertragstests

- **Akteur:** Antigravity / Gemini (`WORKSTATION-LG`)
- **Version:** `0.2.2`
- **Typ:** `GITHUBBOT_ONE_REPO_CLEANER` (Pfad A)
- **Repo:** `dev-bricks/app-rotator` (`C:\_Local_DEV\repos\app-rotator`, Branch `master`, Remote `https://github.com/dev-bricks/app-rotator.git`)
- **Auswahlgrund:** Laut `_tools/repo_radar.py candidates --path a` berechtigter Top-Kandidat auf Default-Branch `master` (Ruhezustand 46.4h > 24h Cooldown, letzte Pfad-A-Prüfung am 2026-09-08), 100% sauberer Git-Zustand, 0 aktive Locks.

### Durchgeführte Maßnahmen:

1. **Python- & Code-Hygiene:**
   - `src/app_rotator/config.py`: Redundantes `try-except FileNotFoundError: pass` durch `contextlib.suppress(FileNotFoundError)` ersetzt.
   - `src/app_rotator/processes.py`: Verschachtelte `if/elif`-Prüfung in `process_matches` vereinfacht (SIM114/SIM102/SIM106).
   - `tests/test_processes.py`: `dict(...)` Aufruf durch Python Dict-Literal ersetzt (C408).
   - `tests/test_controller.py`: `try/except/assert` durch idiomatischen `pytest.raises(ControllerError, match=...)` Kontextmanager ersetzt (PT017).
   - `tests/test_engine.py`: `try/except/assert` durch idiomatischen `pytest.raises(RuntimeError, match=...)` Kontextmanager ersetzt (PT017).

2. **PEP 621 Standard-Metadaten & Ruff-Konfiguration (`pyproject.toml`):**
   - `[project.urls]` um `"Bug Tracker"` ergänzt.
   - `classifiers` um `"Topic :: System :: Monitoring"` ergänzt.
   - `[tool.ruff.lint].select` um erweiterte Regelsätze `["E", "F", "W", "I", "UP", "B", "SIM", "C4", "PT", "RUF"]` ausgebaut.

3. **CI-Workflow-Härtung (`.github/workflows/tests.yml`):**
   - `timeout-minutes: 15` für den Testjob ergänzt, um hängende Runner zuverlässig abzufangen.

4. **Multi-Host Gitignore-Härtung (`.gitignore`):**
   - Ausschlussmuster gegen Multi-Host-Sync-Konflikte (`*-WORKSTATION*`, `*-ASUS-GEI*`, `* (kopie)*`, `* (copy)*`), Caches (`.mypy_cache/`, `.tox/`), IDE-Dateien (`.idea/`, `.vscode/`) und Betriebssystem-Artefakte (`Thumbs.db`, `.DS_Store`) gehärtet.

5. **Dokumentation, Badges & LLM-Kontext:**
   - Badges in `README.md` und `README_de.md` auf Stand `2026--09--11` synchronisiert.
   - `llms.txt` Last-checked Zeitstempel auf `2026-09-11` synchronisiert.
   - `CHANGELOG.md` um Pfad-A-Hygiene-Eintrag ergänzt.

6. **Vertragstest-Erweiterung (`tests/test_metadata.py`):**
   - Testsuite um Prüfungen für CI-Timeout-Guardrails, Bug Tracker URL, Topic-Klassifizierer, erweiterte Gitignore-Muster und aktuellen Prüfzeitstempel erweitert.
